# KRP 周报

**周期**: 2026-03-10 ~ 2026-03-14

---

## ● 本周完成的工作

1. **鸿蒙Editor加载优化**：
   - 在压缩Image的顶层方法前后增加日志，收集平均处理时间
   - 调整多线程压缩分块，如16->64（未看出提升）
   - 利用hash值做缓存，避免重复贴图（未看出提升）
   - 尝试了调整线程分块、跳过冗余格式转换、预取下一个Mipmap、SIMD拷贝方案
   - 目前只有SIMD拷贝有效果
   - 贴图压缩只缩短了1/6的时间。思路有点问题
   - 调整了log：在贴图压缩，脚本编译，着色器编译等阶段，加log并存储到log.txt里
   - 本地测试将压缩算法切成Astc快了8-10倍
   - 修复了鸿蒙项目打开慢的问题

2. **ASAN支持**：
   - hwasan改动进feature_test流水线，client打包有报错（跟ASEPackageManagerHelper.cs有关）
   - publish asan注释掉GCloudActivity.java有loadPluginByReflection("tersafe"），tersafe依然被加载
   - publish asan: 确认libtersafe.so在gcloudsdk-tsssdk.androidlib.aar中，将其删除后，报错
   - 确认GCloudActivity.java修改后的soc.aar没有被正确加载
   - 修复了publish asan和libtersafe冲突的问题
   - 修复了publish asan CI，出的包正常运行
   - 修复了hwasan流水线，能抓取引擎源码

3. **性能分析**：
   - 完善测试文档：添加更多的log，并加上cpu的开销图，附上优化的代码；并进一步分析瓶颈

---

## ● 下周计划

1. 贴图压缩的memcpy通过simd指令优化能快1/6，可以调研mac平台是否都支持
2. 补充一些hwasan回归范围文档
3. 查看ios asan以及编辑器asan等问题
4. 修复压缩算法的问题，并查看其他瓶颈问题
5. 记录publish asan的修复过程
6. 出log后，继续分析其他阶段的耗时

---

*此周报由自动化系统生成*
