# 开发任务列表：日报解析增强

## 📋 任务概览

**项目名称**: 日报解析增强
**版本**: 2.0
**开始日期**: 2026-03-08
**预计完成**: 2026-03-09
**总预估工时**: 6.5小时

---

## 🎯 任务优先级说明

- **P0**: 核心功能，必须完成
- **P1**: 重要功能，应该完成
- **P2**: 次要功能，可以延后
- **P3**: 优化项，可选

---

## 📝 任务列表

### 【P0-001】创建缩进分析工具模块

**模块**: `src/core/indent_analyzer.py`
**优先级**: P0
**预估工时**: 1小时
**依赖**: 无

#### 任务描述
创建独立的缩进分析工具类，提供缩进级别计算和风格检测功能。

#### 实现步骤
1. 创建文件 `src/core/indent_analyzer.py`
2. 实现 `IndentAnalyzer` 类
3. 实现 `calculate_indent_level()` 方法
4. 实现 `detect_indent_style()` 方法
5. 实现 `normalize_indent()` 方法（可选）

#### 代码实现

```python
"""
缩进分析工具

分析文本缩进，支持多种缩进风格
"""

from typing import List
from math import gcd
from functools import reduce


class IndentAnalyzer:
    """缩进分析器"""
    
    @staticmethod
    def calculate_indent_level(line: str, spaces_per_level: int = 4) -> int:
        """
        计算行的缩进级别
        
        Args:
            line: 文本行
            spaces_per_level: 每级缩进的空格数（默认4）
        
        Returns:
            缩进级别（0表示无缩进）
        
        Example:
            >>> IndentAnalyzer.calculate_indent_level("    任务描述")
            1
            >>> IndentAnalyzer.calculate_indent_level("        子项")
            2
            >>> IndentAnalyzer.calculate_indent_level("任务标题")
            0
        """
        stripped = line.lstrip()
        if not stripped:
            return 0
        
        spaces = len(line) - len(stripped)
        return spaces // spaces_per_level
    
    @staticmethod
    def detect_indent_style(lines: List[str]) -> int:
        """
        检测文本使用的缩进风格
        
        通过分析所有缩进行的空格数，计算GCD来判断缩进风格。
        
        Args:
            lines: 文本行列表
        
        Returns:
            每级缩进的空格数（2或4）
        
        Example:
            >>> lines = ["任务", "  描述", "    子项"]
            >>> IndentAnalyzer.detect_indent_style(lines)
            2
        """
        indent_values = []
        
        for line in lines:
            stripped = line.lstrip()
            if stripped and stripped != line:
                spaces = len(line) - len(stripped)
                indent_values.append(spaces)
        
        if not indent_values:
            return 4  # 默认4空格
        
        # 计算GCD
        gcd_value = reduce(gcd, indent_values)
        
        # 判断缩进风格
        if gcd_value == 2:
            return 2
        else:
            return 4
    
    @staticmethod
    def normalize_indent(line: str, from_style: int, to_style: int = 4) -> str:
        """
        规范化缩进风格
        
        Args:
            line: 文本行
            from_style: 原始缩进风格（空格数）
            to_style: 目标缩进风格（空格数）
        
        Returns:
            规范化后的文本行
        
        Example:
            >>> IndentAnalyzer.normalize_indent("  描述", 2, 4)
            "    描述"
        """
        stripped = line.lstrip()
        if not stripped:
            return line
        
        # 计算原始缩进级别
        spaces = len(line) - len(stripped)
        indent_level = spaces // from_style
        
        # 生成新的缩进
        new_indent = " " * (indent_level * to_style)
        
        return new_indent + stripped
    
    @staticmethod
    def get_leading_spaces(line: str) -> int:
        """
        获取行首的空格数
        
        Args:
            line: 文本行
        
        Returns:
            空格数量
        """
        stripped = line.lstrip()
        if not stripped:
            return 0
        return len(line) - len(stripped)
```

#### 验收标准
- ✅ 可以正确计算0级、1级、2级缩进
- ✅ 可以检测2空格和4空格缩进风格
- ✅ 包含完整的类型注解
- ✅ 包含文档字符串和示例
- ✅ 单元测试覆盖率 > 90%

#### 测试用例
```python
def test_calculate_indent_level():
    assert IndentAnalyzer.calculate_indent_level("任务") == 0
    assert IndentAnalyzer.calculate_indent_level("    描述") == 1
    assert IndentAnalyzer.calculate_indent_level("        子项") == 2

def test_detect_indent_style():
    lines_2space = ["任务", "  描述", "    子项"]
    assert IndentAnalyzer.detect_indent_style(lines_2space) == 2
    
    lines_4space = ["任务", "    描述", "        子项"]
    assert IndentAnalyzer.detect_indent_style(lines_4space) == 4
```

---

### 【P0-002】创建类型定义模块

**模块**: `src/core/types.py`
**优先级**: P0
**预估工时**: 0.5小时
**依赖**: 无

#### 任务描述
定义日报解析系统的核心数据类型和结构。

#### 实现步骤
1. 创建文件 `src/core/types.py`
2. 使用 `TypedDict` 定义数据结构
3. 添加类型别名

#### 代码实现

```python
"""
日报数据类型定义

定义日报解析系统的核心数据结构
"""

from typing import TypedDict, List, Union, Dict


class DescriptionGroup(TypedDict):
    """描述分组（带标签的嵌套结构）"""
    label: str
    items: List[str]


# 描述项可以是字符串或分组
DescriptionItem = Union[str, DescriptionGroup]


class Task(TypedDict):
    """任务对象"""
    title: str
    description: List[DescriptionItem]


class MemberTasks(TypedDict):
    """成员任务数据"""
    today: List[Task]
    tomorrow: List[Task]


class DailyReport(TypedDict):
    """日报数据结构"""
    date: str
    version: str
    members: Dict[str, MemberTasks]


# 向后兼容：旧格式任务（字符串）
LegacyTask = str


class LegacyMemberTasks(TypedDict):
    """旧格式成员任务数据"""
    today: List[LegacyTask]
    tomorrow: List[LegacyTask]


class LegacyDailyReport(TypedDict):
    """旧格式日报数据结构"""
    date: str
    version: str
    members: Dict[str, LegacyMemberTasks]
```

#### 验收标准
- ✅ 所有数据结构使用 `TypedDict` 定义
- ✅ 类型注解完整准确
- ✅ 包含向后兼容类型定义
- ✅ 包含文档字符串

---

### 【P0-003】创建任务解析器模块

**模块**: `src/core/task_parser.py`
**优先级**: P0
**预估工时**: 1.5小时
**依赖**: P0-001, P0-002

#### 任务描述
实现任务和描述的解析逻辑，支持嵌套结构。

#### 实现步骤
1. 创建文件 `src/core/task_parser.py`
2. 实现 `TaskParser` 类
3. 实现 `parse_task_with_description()` 方法
4. 实现 `parse_description_item()` 方法
5. 实现辅助方法 `_is_labeled_group()`

#### 代码实现

```python
"""
任务解析器

解析任务及其描述结构
"""

from typing import List, Tuple, Union, Dict
from .indent_analyzer import IndentAnalyzer
from .types import Task, DescriptionGroup, DescriptionItem


class TaskParser:
    """任务解析器"""
    
    def __init__(self, indent_analyzer: IndentAnalyzer = None):
        """
        初始化任务解析器
        
        Args:
            indent_analyzer: 缩进分析器实例（可选）
        """
        self.indent_analyzer = indent_analyzer or IndentAnalyzer()
    
    def parse_task_with_description(
        self,
        lines: List[str],
        start_idx: int,
        spaces_per_level: int = 4
    ) -> Tuple[Task, int]:
        """
        解析任务及其描述
        
        Args:
            lines: 所有文本行
            start_idx: 任务标题所在的行索引
            spaces_per_level: 每级缩进的空格数
        
        Returns:
            (task_dict, next_index)
            - task_dict: {"title": str, "description": List}
            - next_index: 下一个任务的起始索引
        
        Example:
            >>> lines = ["任务标题", "    描述1", "    描述2"]
            >>> task, idx = parser.parse_task_with_description(lines, 0)
            >>> task["title"]
            "任务标题"
            >>> len(task["description"])
            2
        """
        task: Task = {
            "title": lines[start_idx].strip(),
            "description": []
        }
        
        idx = start_idx + 1
        
        while idx < len(lines):
            line = lines[idx]
            
            # 跳过空行
            if not line.strip():
                idx += 1
                continue
            
            # 计算缩进级别
            indent = self.indent_analyzer.calculate_indent_level(
                line, spaces_per_level
            )
            
            # 缩进级别0，任务结束
            if indent == 0:
                break
            
            # 解析描述项
            desc_item, idx = self.parse_description_item(
                lines, idx, indent, spaces_per_level
            )
            task["description"].append(desc_item)
        
        return task, idx
    
    def parse_description_item(
        self,
        lines: List[str],
        idx: int,
        current_indent: int,
        spaces_per_level: int = 4
    ) -> Tuple[DescriptionItem, int]:
        """
        解析单个描述项
        
        Args:
            lines: 所有文本行
            idx: 当前行索引
            current_indent: 当前缩进级别
            spaces_per_level: 每级缩进的空格数
        
        Returns:
            (description_item, next_index)
            - description_item: 字符串或 {"label": str, "items": List}
            - next_index: 下一个描述项的索引
        """
        content = lines[idx].strip()
        
        # 检查是否有子项（下一行缩进更深）
        if self._has_child_items(lines, idx, current_indent, spaces_per_level):
            # 当前项是标签，构建分组
            group: DescriptionGroup = {
                "label": content,
                "items": []
            }
            
            idx += 1
            child_indent = current_indent + 1
            
            # 收集所有直接子项
            while idx < len(lines):
                line = lines[idx]
                
                # 跳过空行
                if not line.strip():
                    idx += 1
                    continue
                
                line_indent = self.indent_analyzer.calculate_indent_level(
                    line, spaces_per_level
                )
                
                # 缩进减少或为0，子项结束
                if line_indent <= current_indent:
                    break
                
                # 只收集直接子项（缩进级别 = current_indent + 1）
                if line_indent == child_indent:
                    group["items"].append(line.strip())
                
                idx += 1
            
            return group, idx
        
        # 简单文本描述
        return content, idx + 1
    
    def _has_child_items(
        self,
        lines: List[str],
        idx: int,
        current_indent: int,
        spaces_per_level: int
    ) -> bool:
        """
        判断当前行是否有子项
        
        Args:
            lines: 所有文本行
            idx: 当前行索引
            current_indent: 当前缩进级别
            spaces_per_level: 每级缩进的空格数
        
        Returns:
            True 如果下一行缩进更深
        """
        if idx + 1 >= len(lines):
            return False
        
        next_line = lines[idx + 1]
        if not next_line.strip():
            # 如果下一行是空行，继续向后查找
            for i in range(idx + 2, len(lines)):
                if lines[i].strip():
                    next_line = lines[i]
                    break
                if i - idx > 3:  # 最多查找3行
                    return False
        
        next_indent = self.indent_analyzer.calculate_indent_level(
            next_line, spaces_per_level
        )
        
        return next_indent > current_indent
```

#### 验收标准
- ✅ 可以解析简单任务（无描述）
- ✅ 可以解析带文本描述的任务
- ✅ 可以解析带嵌套描述的任务
- ✅ 正确处理空行
- ✅ 单元测试覆盖率 > 90%

#### 测试用例
```python
def test_parse_simple_task():
    lines = ["简单任务"]
    parser = TaskParser()
    task, idx = parser.parse_task_with_description(lines, 0)
    
    assert task["title"] == "简单任务"
    assert task["description"] == []
    assert idx == 1

def test_parse_task_with_nested_description():
    lines = [
        "搞打包内存高的问题",
        "    效果",
        "        BuildClient从40+GB降到10+GB",
        "        平台资源生成从30+GB降到10+GB"
    ]
    parser = TaskParser()
    task, idx = parser.parse_task_with_description(lines, 0)
    
    assert task["title"] == "搞打包内存高的问题"
    assert len(task["description"]) == 1
    assert task["description"][0]["label"] == "效果"
    assert len(task["description"][0]["items"]) == 2
```

---

### 【P0-004】创建数据标准化器模块

**模块**: `src/core/data_normalizer.py`
**优先级**: P0
**预估工时**: 1小时
**依赖**: P0-002

#### 任务描述
实现数据格式标准化，支持新旧格式转换。

#### 实现步骤
1. 创建文件 `src/core/data_normalizer.py`
2. 实现 `DataNormalizer` 类
3. 实现 `normalize_task()` 方法
4. 实现 `normalize_report()` 方法
5. 实现 `is_legacy_format()` 方法

#### 代码实现

```python
"""
数据标准化器

标准化日报数据格式，支持向后兼容
"""

from typing import Dict, Union, List
from .types import (
    Task, DailyReport, LegacyDailyReport,
    DescriptionItem
)


class DataNormalizer:
    """数据标准化器"""
    
    @staticmethod
    def normalize_task(task: Union[str, Task]) -> Task:
        """
        将任务标准化为新格式
        
        Args:
            task: 旧格式（字符串）或新格式（字典）
        
        Returns:
            标准化后的任务字典
        
        Example:
            >>> DataNormalizer.normalize_task("简单任务")
            {"title": "简单任务", "description": []}
        """
        if isinstance(task, str):
            return {
                "title": task,
                "description": []
            }
        
        # 已经是新格式，验证结构
        if "title" not in task:
            raise ValueError("任务对象缺少title字段")
        
        if "description" not in task:
            task["description"] = []
        
        return task
    
    @staticmethod
    def normalize_member_tasks(member_data: Dict) -> Dict:
        """
        标准化成员任务数据
        
        Args:
            member_data: 成员任务数据
        
        Returns:
            标准化后的成员任务数据
        """
        normalized = {
            "today": [],
            "tomorrow": []
        }
        
        for section in ["today", "tomorrow"]:
            if section in member_data:
                tasks = member_data[section]
                normalized[section] = [
                    DataNormalizer.normalize_task(task)
                    for task in tasks
                ]
        
        return normalized
    
    @staticmethod
    def normalize_report(report: Union[DailyReport, LegacyDailyReport]) -> DailyReport:
        """
        标准化整个日报
        
        Args:
            report: 原始日报数据
        
        Returns:
            标准化后的日报（版本2.0）
        """
        normalized: DailyReport = {
            "date": report.get("date", ""),
            "version": "2.0",
            "members": {}
        }
        
        for member, member_data in report.get("members", {}).items():
            normalized["members"][member] = DataNormalizer.normalize_member_tasks(
                member_data
            )
        
        return normalized
    
    @staticmethod
    def is_legacy_format(report: Dict) -> bool:
        """
        判断是否是旧版格式
        
        Args:
            report: 日报数据
        
        Returns:
            True 如果任务使用字符串格式
        """
        members = report.get("members", {})
        
        for member_data in members.values():
            for section in ["today", "tomorrow"]:
                tasks = member_data.get(section, [])
                if tasks and isinstance(tasks[0], str):
                    return True
        
        return False
    
    @staticmethod
    def to_legacy_task(task: Task) -> str:
        """
        将新格式任务转换为旧格式（丢弃描述）
        
        Args:
            task: 新格式任务对象
        
        Returns:
            任务标题字符串
        """
        return task.get("title", "")
    
    @staticmethod
    def to_legacy_report(report: DailyReport) -> LegacyDailyReport:
        """
        将新格式日报转换为旧格式（丢弃描述）
        
        Args:
            report: 新格式日报
        
        Returns:
            旧格式日报
        """
        legacy: LegacyDailyReport = {
            "date": report.get("date", ""),
            "version": "1.0",
            "members": {}
        }
        
        for member, member_data in report.get("members", {}).items():
            legacy["members"][member] = {
                "today": [
                    DataNormalizer.to_legacy_task(task)
                    for task in member_data.get("today", [])
                ],
                "tomorrow": [
                    DataNormalizer.to_legacy_task(task)
                    for task in member_data.get("tomorrow", [])
                ]
            }
        
        return legacy
```

#### 验收标准
- ✅ 可以将字符串任务转换为新格式
- ✅ 可以检测旧版格式
- ✅ 支持双向转换
- ✅ 单元测试覆盖率 > 90%

---

### 【P1-005】修改解析器主模块

**模块**: `src/core/parser.py`
**优先级**: P1
**预估工时**: 1.5小时
**依赖**: P0-001, P0-002, P0-003, P0-004

#### 任务描述
修改解析器主入口，集成新的工具模块。

#### 实现步骤
1. 修改 `src/core/parser.py`
2. 导入新的工具模块
3. 修改 `parse_daily_report()` 函数
4. 保留旧的辅助函数
5. 更新版本号为2.0

#### 主要修改点

**修改1: 导入新模块**

```python
# 在文件顶部添加导入
from .indent_analyzer import IndentAnalyzer
from .task_parser import TaskParser
from .data_normalizer import DataNormalizer
from .types import Task, DailyReport
```

**修改2: 更新 `parse_daily_report()` 函数**

```python
def parse_daily_report(markdown_text: str, date: Optional[str] = None) -> DailyReport:
    """
    解析markdown格式日报（支持任务描述）
    
    Args:
        markdown_text: markdown格式的日报文本
        date: 日期字符串（YYYYMMDD格式），默认为今天
    
    Returns:
        解析后的日报数据结构（版本2.0）
    
    Version Changes:
        v2.0: 支持任务描述和嵌套结构
        v1.0: 仅支持简单任务列表
    """
    if date is None:
        date = datetime.now().strftime("%Y%m%d")
    
    # 初始化工具
    indent_analyzer = IndentAnalyzer()
    task_parser = TaskParser(indent_analyzer)
    
    # 初始化结果
    result: DailyReport = {
        "date": date,
        "version": "2.0",
        "members": {}
    }
    
    # 按行分割（保留原始缩进！）
    lines = markdown_text.split('\n')
    
    # 检测缩进风格
    spaces_per_level = indent_analyzer.detect_indent_style(lines)
    
    current_member = None
    current_section = None  # "today" or "tomorrow"
    
    idx = 0
    while idx < len(lines):
        original_line = lines[idx]
        stripped_line = original_line.strip()
        
        # 跳过空行
        if not stripped_line:
            idx += 1
            continue
        
        # 检查是否是成员行（格式：成员名:）
        member_match = re.match(r'^([A-Z]{2,4})\s*:', stripped_line)
        if member_match:
            current_member = member_match.group(1)
            if current_member not in result["members"]:
                result["members"][current_member] = {"today": [], "tomorrow": []}
            current_section = None
            idx += 1
            continue
        
        # 检查是否是"今"或"明"标记
        if '今' in stripped_line and ':' in stripped_line:
            current_section = "today"
            idx += 1
            continue
        
        if '明' in stripped_line and ':' in stripped_line:
            current_section = "tomorrow"
            idx += 1
            continue
        
        # 计算缩进级别
        indent = indent_analyzer.calculate_indent_level(
            original_line, spaces_per_level
        )
        
        # 缩进级别0，可能是任务标题
        if indent == 0 and current_member and current_section:
            # 检查是否是标题行（跳过markdown标题）
            if stripped_line.startswith('#'):
                idx += 1
                continue
            
            # 去除任务标记符（- • 等）
            task_match = re.match(r'^[-•]\s*(.+)', stripped_line)
            if task_match:
                task_title = task_match.group(1).strip()
            else:
                task_title = stripped_line
            
            if task_title:
                # 解析任务及描述
                task, idx = task_parser.parse_task_with_description(
                    lines, idx, spaces_per_level
                )
                task["title"] = task_title  # 使用清理后的标题
                result["members"][current_member][current_section].append(task)
            else:
                idx += 1
        else:
            idx += 1
    
    return result
```

#### 验收标准
- ✅ 保留原始行的缩进信息
- ✅ 正确解析带描述的任务
- ✅ 版本号升级为2.0
- ✅ 向后兼容旧格式
- ✅ 所有现有测试通过

---

### 【P1-006】修改日报收集器模块

**模块**: `src/modules/daily_collector.py`
**优先级**: P1
**预估工时**: 1小时
**依赖**: P0-004, P1-005

#### 任务描述
修改收集器以适配新数据结构，更新markdown输出格式。

#### 实现步骤
1. 修改 `src/modules/daily_collector.py`
2. 更新 `_convert_to_markdown()` 方法
3. 导入 `DataNormalizer`
4. 保持向后兼容

#### 主要修改点

**修改1: 导入DataNormalizer**

```python
from ..core.data_normalizer import DataNormalizer
```

**修改2: 更新 `_convert_to_markdown()` 方法**

```python
def _convert_to_markdown(self, report: Dict) -> str:
    """
    转换日报为markdown格式（支持任务描述）
    
    Args:
        report: 日报数据
    
    Returns:
        markdown文本（保留缩进结构）
    """
    lines = [f"# 日报 - {report.get('date', '')}", ""]
    
    for member, data in report.get("members", {}).items():
        lines.append(f"## {member}")
        lines.append("")
        
        # 今日任务
        lines.append("### 今日完成")
        for task in data.get("today", []):
            # 标准化任务（向后兼容）
            task = DataNormalizer.normalize_task(task)
            
            # 任务标题
            lines.append(f"- {task['title']}")
            
            # 任务描述
            for desc in task.get("description", []):
                if isinstance(desc, str):
                    # 简单描述
                    lines.append(f"    {desc}")
                else:
                    # 带标签的分组
                    lines.append(f"    {desc['label']}")
                    for item in desc.get("items", []):
                        lines.append(f"        {item}")
        
        lines.append("")
        
        # 明日计划
        lines.append("### 明日计划")
        for task in data.get("tomorrow", []):
            # 标准化任务（向后兼容）
            task = DataNormalizer.normalize_task(task)
            
            # 任务标题
            lines.append(f"- {task['title']}")
            
            # 任务描述（如果有）
            for desc in task.get("description", []):
                if isinstance(desc, str):
                    lines.append(f"    {desc}")
                else:
                    lines.append(f"    {desc['label']}")
                    for item in desc.get("items", []):
                        lines.append(f"        {item}")
        
        lines.append("")
    
    return "\n".join(lines)
```

#### 验收标准
- ✅ 正确输出带描述的任务
- ✅ 保留缩进结构
- ✅ 向后兼容旧数据
- ✅ 所有现有功能正常

---

### 【P2-007】编写单元测试

**模块**: `src/tests/`
**优先级**: P2
**预估工时**: 1.5小时
**依赖**: P0-001, P0-002, P0-003, P0-004

#### 任务描述
为新模块编写完整的单元测试。

#### 测试文件列表

1. **test_indent_analyzer.py** - 缩进分析器测试
2. **test_task_parser.py** - 任务解析器测试
3. **test_data_normalizer.py** - 数据标准化器测试
4. **test_parser_integration.py** - 解析器集成测试

#### 实现步骤

**步骤1: 创建测试目录**

```bash
mkdir -p src/tests
touch src/tests/__init__.py
```

**步骤2: 编写 `test_indent_analyzer.py`**

```python
"""
缩进分析器单元测试
"""

import pytest
from ..core.indent_analyzer import IndentAnalyzer


class TestIndentAnalyzer:
    """缩进分析器测试"""
    
    def test_calculate_indent_level_no_indent(self):
        """测试无缩进"""
        assert IndentAnalyzer.calculate_indent_level("任务标题") == 0
        assert IndentAnalyzer.calculate_indent_level("") == 0
        assert IndentAnalyzer.calculate_indent_level("   ") == 0  # 只有空格
    
    def test_calculate_indent_level_one_level(self):
        """测试1级缩进（4空格）"""
        assert IndentAnalyzer.calculate_indent_level("    描述") == 1
        assert IndentAnalyzer.calculate_indent_level("    任务详情") == 1
    
    def test_calculate_indent_level_two_levels(self):
        """测试2级缩进（8空格）"""
        assert IndentAnalyzer.calculate_indent_level("        子项") == 2
        assert IndentAnalyzer.calculate_indent_level("        详细说明") == 2
    
    def test_calculate_indent_level_custom_spaces(self):
        """测试自定义空格数"""
        # 2空格缩进
        assert IndentAnalyzer.calculate_indent_level("  描述", 2) == 1
        assert IndentAnalyzer.calculate_indent_level("    子项", 2) == 2
    
    def test_detect_indent_style_2spaces(self):
        """检测2空格缩进风格"""
        lines = [
            "任务",
            "  描述",
            "    子项"
        ]
        assert IndentAnalyzer.detect_indent_style(lines) == 2
    
    def test_detect_indent_style_4spaces(self):
        """检测4空格缩进风格"""
        lines = [
            "任务",
            "    描述",
            "        子项"
        ]
        assert IndentAnalyzer.detect_indent_style(lines) == 4
    
    def test_detect_indent_style_no_indent(self):
        """检测无缩进的情况"""
        lines = ["任务1", "任务2", "任务3"]
        assert IndentAnalyzer.detect_indent_style(lines) == 4  # 默认
    
    def test_normalize_indent(self):
        """测试缩进规范化"""
        # 2空格转4空格
        assert IndentAnalyzer.normalize_indent("  描述", 2, 4) == "    描述"
        assert IndentAnalyzer.normalize_indent("    子项", 2, 4) == "        子项"
```

**步骤3: 编写 `test_task_parser.py`**

```python
"""
任务解析器单元测试
"""

import pytest
from ..core.task_parser import TaskParser
from ..core.indent_analyzer import IndentAnalyzer


class TestTaskParser:
    """任务解析器测试"""
    
    def setup_method(self):
        """测试初始化"""
        self.parser = TaskParser(IndentAnalyzer())
    
    def test_parse_simple_task(self):
        """测试简单任务（无描述）"""
        lines = ["简单任务"]
        task, idx = self.parser.parse_task_with_description(lines, 0)
        
        assert task["title"] == "简单任务"
        assert task["description"] == []
        assert idx == 1
    
    def test_parse_task_with_single_description(self):
        """测试带单个描述的任务"""
        lines = [
            "任务标题",
            "    这是描述"
        ]
        task, idx = self.parser.parse_task_with_description(lines, 0)
        
        assert task["title"] == "任务标题"
        assert len(task["description"]) == 1
        assert task["description"][0] == "这是描述"
        assert idx == 2
    
    def test_parse_task_with_multiple_descriptions(self):
        """测试带多个描述的任务"""
        lines = [
            "任务标题",
            "    描述1",
            "    描述2",
            "    描述3"
        ]
        task, idx = self.parser.parse_task_with_description(lines, 0)
        
        assert task["title"] == "任务标题"
        assert len(task["description"]) == 3
        assert task["description"][0] == "描述1"
        assert task["description"][1] == "描述2"
        assert task["description"][2] == "描述3"
    
    def test_parse_task_with_nested_description(self):
        """测试带嵌套描述的任务"""
        lines = [
            "搞打包内存高的问题",
            "    效果",
            "        BuildClient从40+GB降到10+GB",
            "        平台资源生成从30+GB降到10+GB"
        ]
        task, idx = self.parser.parse_task_with_description(lines, 0)
        
        assert task["title"] == "搞打包内存高的问题"
        assert len(task["description"]) == 1
        
        group = task["description"][0]
        assert group["label"] == "效果"
        assert len(group["items"]) == 2
        assert "BuildClient" in group["items"][0]
        assert "平台资源" in group["items"][1]
    
    def test_parse_task_with_mixed_descriptions(self):
        """测试混合描述（文本+嵌套）"""
        lines = [
            "任务标题",
            "    普通描述",
            "    分组标签",
            "        子项1",
            "        子项2"
        ]
        task, idx = self.parser.parse_task_with_description(lines, 0)
        
        assert len(task["description"]) == 2
        assert task["description"][0] == "普通描述"
        assert task["description"][1]["label"] == "分组标签"
    
    def test_parse_task_with_empty_lines(self):
        """测试包含空行的任务"""
        lines = [
            "任务标题",
            "",
            "    描述1",
            "",
            "    描述2"
        ]
        task, idx = self.parser.parse_task_with_description(lines, 0)
        
        assert task["title"] == "任务标题"
        assert len(task["description"]) == 2
```

**步骤4: 编写 `test_data_normalizer.py`**

```python
"""
数据标准化器单元测试
"""

import pytest
from ..core.data_normalizer import DataNormalizer


class TestDataNormalizer:
    """数据标准化器测试"""
    
    def test_normalize_task_string(self):
        """测试字符串任务标准化"""
        task = DataNormalizer.normalize_task("简单任务")
        
        assert task == {
            "title": "简单任务",
            "description": []
        }
    
    def test_normalize_task_dict(self):
        """测试字典任务标准化"""
        task = DataNormalizer.normalize_task({
            "title": "任务",
            "description": ["描述"]
        })
        
        assert task == {
            "title": "任务",
            "description": ["描述"]
        }
    
    def test_normalize_task_missing_description(self):
        """测试缺少description字段"""
        task = DataNormalizer.normalize_task({
            "title": "任务"
        })
        
        assert task == {
            "title": "任务",
            "description": []
        }
    
    def test_is_legacy_format_true(self):
        """测试检测旧格式（字符串任务）"""
        report = {
            "members": {
                "HLQ": {
                    "today": ["任务1", "任务2"],
                    "tomorrow": []
                }
            }
        }
        
        assert DataNormalizer.is_legacy_format(report) == True
    
    def test_is_legacy_format_false(self):
        """测试检测新格式（对象任务）"""
        report = {
            "members": {
                "HLQ": {
                    "today": [
                        {"title": "任务", "description": []}
                    ],
                    "tomorrow": []
                }
            }
        }
        
        assert DataNormalizer.is_legacy_format(report) == False
    
    def test_normalize_report(self):
        """测试标准化整个日报"""
        legacy_report = {
            "date": "20260308",
            "version": "1.0",
            "members": {
                "HLQ": {
                    "today": ["任务1"],
                    "tomorrow": ["任务2"]
                }
            }
        }
        
        normalized = DataNormalizer.normalize_report(legacy_report)
        
        assert normalized["version"] == "2.0"
        assert len(normalized["members"]["HLQ"]["today"]) == 1
        assert normalized["members"]["HLQ"]["today"][0]["title"] == "任务1"
```

**步骤5: 编写 `test_parser_integration.py`**

```python
"""
解析器集成测试
"""

import pytest
from ..core.parser import parse_daily_report


class TestParserIntegration:
    """解析器集成测试"""
    
    def test_parse_complete_report_with_descriptions(self):
        """测试解析完整日报（带描述）"""
        markdown_text = """
HLQ:
今: 搞打包内存高的问题
    死锁的解决方案测试通过, 不再死锁
    效果
        BuildClient从40+GB降到10+GB
        平台资源生成从30+GB降到10+GB
明: 继续优化
"""
        
        report = parse_daily_report(markdown_text, "20260308")
        
        # 验证基本结构
        assert report["date"] == "20260308"
        assert report["version"] == "2.0"
        assert "HLQ" in report["members"]
        
        # 验证今日任务
        today_tasks = report["members"]["HLQ"]["today"]
        assert len(today_tasks) == 1
        
        task = today_tasks[0]
        assert task["title"] == "搞打包内存高的问题"
        assert len(task["description"]) == 2
        
        # 验证嵌套描述
        effect_group = task["description"][1]
        assert effect_group["label"] == "效果"
        assert len(effect_group["items"]) == 2
    
    def test_parse_legacy_format(self):
        """测试解析旧格式日报"""
        markdown_text = """
HLQ:
今: 
- 任务1
- 任务2
明: 
- 任务3
"""
        
        report = parse_daily_report(markdown_text, "20260308")
        
        # 应该能够解析
        assert "HLQ" in report["members"]
        assert len(report["members"]["HLQ"]["today"]) == 2
        assert report["members"]["HLQ"]["today"][0]["title"] == "任务1"
```

#### 验收标准
- ✅ 所有测试通过
- ✅ 代码覆盖率 > 90%
- ✅ 边界情况测试完整

---

### 【P2-008】更新技能文档

**模块**: `~/.openclaw/skills/daily-report-collector/SKILL.md`
**优先级**: P2
**预估工时**: 0.5小时
**依赖**: P1-006

#### 任务描述
更新技能文档，说明新的日报格式和用法。

#### 更新内容

1. **添加缩进格式说明**
2. **更新数据结构示例**
3. **添加最佳实践**

#### 文档模板

```markdown
# Daily Report Collector Skill

## 格式说明

### 基本格式

```
成员名:
今: 任务标题
    任务描述（可选）
    效果（可选标签）
        具体效果1
        具体效果2
明: 明日计划
```

### 示例

**简单任务**:
```
HLQ:
今: 修复bug #123
明: 继续开发
```

**带描述的任务**:
```
HLQ:
今: 搞打包内存高的问题
    死锁的解决方案测试通过, 不再死锁
    效果
        BuildClient从40+GB降到10+GB
        平台资源生成从30+GB降到10+GB
明: 继续优化
```

### 缩进规则

- **0级缩进（无缩进）**: 任务标题
- **1级缩进（4空格）**: 任务描述或标签
- **2级缩进（8空格）**: 标签下的子项

### 最佳实践

1. 使用4空格缩进（推荐）
2. 标签后应跟子项
3. 描述简洁明了
```

---

## 📊 任务依赖关系图

```
P0-001 (缩进分析器) ──┐
                      │
P0-002 (类型定义) ────┼──> P0-003 (任务解析器) ──┐
                      │                           │
P0-002 (类型定义) ────┴──> P0-004 (数据标准化) ──┤
                                                  ├──> P1-005 (修改解析器) ──> P1-006 (修改收集器) ──> P2-008 (更新文档)
P0-001, P0-002, P0-003, P0-004 ──────────────────┴──> P2-007 (单元测试)
```

---

## 🎯 里程碑

### 里程碑1: 核心工具层完成 (P0任务)
**时间**: 第1天
**交付物**:
- ✅ 缩进分析器
- ✅ 类型定义
- ✅ 任务解析器
- ✅ 数据标准化器

### 里程碑2: 业务层集成完成 (P1任务)
**时间**: 第2天上午
**交付物**:
- ✅ 解析器修改
- ✅ 收集器修改

### 里程碑3: 测试与文档完成 (P2任务)
**时间**: 第2天下午
**交付物**:
- ✅ 单元测试
- ✅ 技能文档

---

## ✅ 验收标准

### 功能验收
- [ ] 可以正确解析带描述的任务
- [ ] 描述支持文本和嵌套分组
- [ ] 向后兼容旧格式
- [ ] markdown输出保留缩进结构

### 质量验收
- [ ] 单元测试覆盖率 > 90%
- [ ] 所有测试通过
- [ ] 无代码风格警告
- [ ] 类型检查通过

### 文档验收
- [ ] 架构文档完整
- [ ] 开发任务列表完整
- [ ] 技能文档更新
- [ ] 代码注释清晰

---

## 🚀 开发流程建议

### 第1步: 实现核心工具层（3.5小时）
1. P0-001: 创建缩进分析器（1小时）
2. P0-002: 创建类型定义（0.5小时）
3. P0-003: 创建任务解析器（1.5小时）
4. P0-004: 创建数据标准化器（1小时）

### 第2步: 集成到业务层（2.5小时）
1. P1-005: 修改解析器（1.5小时）
2. P1-006: 修改收集器（1小时）

### 第3步: 测试与文档（2小时）
1. P2-007: 编写单元测试（1.5小时）
2. P2-008: 更新文档（0.5小时）

---

## 📝 注意事项

### 开发注意事项
1. **保留原始缩进**: 在解析过程中，务必保留原始行的缩进信息，不要过早调用 `strip()`
2. **向后兼容**: 所有修改必须支持旧版字符串格式
3. **类型安全**: 使用 TypedDict 确保数据结构类型安全
4. **测试优先**: 先写测试用例，再实现功能

### 常见陷阱
1. ❌ 过早 `strip()` 导致缩进丢失
2. ❌ 忘记处理空行
3. ❌ 嵌套层级判断错误
4. ❌ 向后兼容性测试不足

---

## 🔧 调试技巧

### 打印缩进信息
```python
for idx, line in enumerate(lines):
    indent = IndentAnalyzer.calculate_indent_level(line)
    print(f"Line {idx}: indent={indent}, content='{line.strip()}'")
```

### 验证数据结构
```python
import json
print(json.dumps(report, indent=2, ensure_ascii=False))
```

### 测试单个任务
```python
from src.core.task_parser import TaskParser

lines = [
    "任务标题",
    "    描述1",
    "    标签",
    "        子项1",
    "        子项2"
]

parser = TaskParser()
task, idx = parser.parse_task_with_description(lines, 0)
print(task)
```

---

## 📚 参考资料

1. **架构文档**: `docs/架构文档_日报解析增强.md`
2. **需求文档**: `docs/需求拆分文档_日报解析增强.md`
3. **原始代码**: `src/core/parser.py`
4. **Python TypedDict**: https://docs.python.org/3/library/typing.html#typing.TypedDict

---

## 🎉 完成标志

当所有任务完成并满足验收标准后，在下方标记完成：

- [ ] 所有P0任务完成
- [ ] 所有P1任务完成
- [ ] 所有P2任务完成
- [ ] 单元测试覆盖率达标
- [ ] 文档更新完成
- [ ] 代码审查通过
- [ ] 可以部署到生产

**完成日期**: _________
**验收人**: _________
